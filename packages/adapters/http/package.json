{
  "name": "@transferx/adapter-http",
  "version": "1.1.0",
  "description": "TransferX generic HTTP adapter — function-based callbacks for any HTTP multipart endpoint",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@transferx/core": "1.1.0"
  }
}
