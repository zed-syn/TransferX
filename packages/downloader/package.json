{
  "name": "@transferx/downloader",
  "version": "1.3.4",
  "description": "IDM-class parallel download engine — multi-connection range downloads with byte-level resume, adaptive concurrency, and streaming writes",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "undici": "^6.21.0"
  },
  "devDependencies": {
    "@types/node": "*",
    "typescript": "*"
  }
}
